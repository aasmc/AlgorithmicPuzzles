package yandex_algo_training.final_contest.second

fun main() {
    val (n, w) = readLine()!!.trim().split(" ").map { it.toInt() }
    val employees = ArrayDeque<Pair<Int, Int>>()
    repeat(n) {

    }
}